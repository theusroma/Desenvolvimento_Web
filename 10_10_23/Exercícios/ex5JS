//Todo restaurante, embora por lei não possa obrigar o cliente a pagar, cobra 10% de comissão para ogarçom. Escreva um programa que leia o valor gasto pelo cliente em um restaurante e mostre o valorda gorjeta e o valor total a ser pago

let valor = Number(prompt("Qual foi o valor gasto?\n"));

let gorjeta = valor * 0.1;

console.log("O valor da gorjeta é de R$:",gorjeta);

console.log('O valor total é de R$:', gorjeta + valor);
